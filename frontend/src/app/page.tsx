import React from "react";
import { DashboardComponent } from "@/components/dashboard";
type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <DashboardComponent />
    </div>
  );
};

export default page;
